name: test Pipeline
'on':
  push:
    branches:
    - main
env:
  NODE_ENV: production
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - name: 📥 Checkout Source
      uses: actions/checkout@v4
