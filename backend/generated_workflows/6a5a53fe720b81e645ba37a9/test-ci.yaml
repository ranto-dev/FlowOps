name: test Pipeline
'on':
  push:
    branches:
    - main
  workflow_dispatch: {}
  repository_dispatch:
    types:
    - flowops_trigger
env:
  NODE_ENV: production
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - name: 📥 Checkout Source
      uses: actions/checkout@v4
